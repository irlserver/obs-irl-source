//! Properties UI and defaults (port of `src/settings.c`). W2-D.

use obs::{Data, Properties};

use crate::source::IrlSource;

pub fn defaults(settings: &Data<'_>) {
    let _ = settings;
    todo!("W2-D")
}

pub fn properties(instance: Option<&IrlSource>) -> Properties {
    let _ = instance;
    todo!("W2-D")
}
